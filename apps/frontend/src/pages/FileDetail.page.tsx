import React from "react";
import MainLayout from "../layouts/main.layout";
import FileView from "../components/File.component";


const FileDetail: React.FC = () => {
  

  return (
    <MainLayout>
      <FileView/>
    </MainLayout>
  );
};

export default FileDetail;
